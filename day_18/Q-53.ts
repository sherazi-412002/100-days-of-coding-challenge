

let studentResult = {
    marks:[86,69,57,91,74],
    subjects:["Bio", "chem", "eng", "math", "urdu"],
    grade:["A","B","C","A+","A"]
}

let {marks,subjects,grade} = studentResult

console.log(`Student got ${marks[0]} marks in ${subjects[0]} with a grade "${grade[0]}"`)